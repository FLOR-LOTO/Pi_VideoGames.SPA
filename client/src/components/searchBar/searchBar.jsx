import "./searchBar.module.css";

const SearchBar = () => {
  return (
    <div className="">
      <form>
        <input placeholder="game" />
        <button>Search</button>
      </form>
    </div>
  );
};

export default SearchBar;
